import {
  createUser,
  hashedPassword,
  getUserByEmailExPw,
  comparePassword,
  getUserByEmail,
  generateJwtToken,
} from "../services/user.service.js";

// get profile
export const getMyProfile = async (req, res) => {
  try {
  } catch (error) {}
};
